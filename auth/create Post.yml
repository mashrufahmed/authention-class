info:
  name: create Post
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/post/create-post
  body:
    type: json
    data: |-
      {
        "title": "hello@gmail.com",
        "description": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
